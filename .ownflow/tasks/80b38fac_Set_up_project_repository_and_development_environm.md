# TEST SHOP - Project Repository Setup & Development Environment

I'll set up a professional, production-ready e-commerce platform with a clean Git workflow, organized backend (Express.js) and frontend (React) boilerplate, and comprehensive development documentation.

## Overview

This deliverable includes:
1. **Git Configuration** - `.gitignore`, branch strategy, and workflow documentation
2. **Backend Setup** - Express.js server with middleware, environment config, logging, and error handling
3. **Frontend Setup** - React project with organized folder structure and build tools
4. **Documentation** - Comprehensive setup and development guides
5. **Scripts** - Convenient npm scripts for development workflow

---

## Architecture

```
test-shop/
├── backend/                 # Node.js/Express API server
│   ├── src/
│   │   ├── middleware/      # Express middleware (auth, logging, error handling)
│   │   ├── routes/          # API route definitions
│   │   ├── controllers/      # Business logic
│   │   ├── models/          # Data models (will use PostgreSQL)
│   │   ├── utils/           # Helper functions
│   │   ├── config/          # Configuration management
│   │   └── app.js           # Express app initialization
│   ├── .env.example         # Environment variables template
│   ├── .gitignore
│   ├── package.json
│   └── server.js            # Server entry point
├── frontend/                # React web application
│   ├── public/
│   ├── src/
│   │   ├── components/      # Reusable React components
│   │   ├── pages/           # Page components
│   │   ├── services/        # API client and utilities
│   │   ├── hooks/           # Custom React hooks
│   │   ├── context/         # React Context (state management)
│   │   ├── styles/          # Global styles
│   │   ├── App.js
│   │   └── index.js
│   ├── .env.example
│   ├── .gitignore
│   ├── package.json
│   └── public/index.html
├── .gitignore               # Root .gitignore
├── README.md                # Project overview
├── SETUP.md                 # Development setup guide
├── GIT_WORKFLOW.md          # Git branching strategy
└── package.json             # Root workspace (optional)
```

---

## Branch Strategy & Git Workflow

**Main branches:**
- `main` - Production-ready code (protected, requires PR reviews)
- `develop` - Integration branch for features (staging/testing)

**Supporting branches:**
- `feature/*` - Feature development (e.g., `feature/product-catalog`)
- `bugfix/*` - Bug fixes (e.g., `bugfix/cart-calculation`)
- `hotfix/*` - Production hotfixes (e.g., `hotfix/payment-error`)

---

## Key Features of This Setup

✅ **Backend:**
- Express.js with modular structure
- Environment-based configuration (development, staging, production)
- Request logging (Morgan middleware)
- Centralized error handling
- CORS and security middleware
- RESTful API structure ready for routes

✅ **Frontend:**
- React with functional components and hooks
- Organized folder structure (components, pages, services, context)
- API service layer for backend communication
- Environment configuration support
- Build optimization with create-react-app

✅ **Development Experience:**
- Separate .env files for configuration
- npm scripts for local development
- Hot-reload for both backend and frontend
- Clear documentation and setup guide

---

## Files

###FILES###
[
  {
    "path": ".gitignore",
    "content": "# Dependencies\nnode_modules/\n/.pnp\n.pnp.js\n\n# Environment variables\n.env\n.env.local\n.env.development.local\n.env.test.local\n.env.production.local\n\n# Build outputs\n/build\n/dist\n.next\nout/\n\n# IDE and editor\n.vscode/\n.idea/\n*.swp\n*.swo\n*~\n.DS_Store\n.env.*.local\n*.log\nnpm-debug.log*\nyarn-debug.log*\nyarn-error.log*\n\n# Testing\n/coverage\n.nyc_output\n\n# OS\nThumbs.db\n.DS_Store\n\n# Lock files (optional - enable if team uses single package manager)\n# package-lock.json\n# yarn.lock\n"
  },
  {
    "path": "README.md",
    "content": "# TEST SHOP - E-Commerce Platform\n\nA modern, full-stack e-commerce platform for a candy shop. Built with React (frontend), Express.js (backend), and PostgreSQL (database).\n\n## 🎯 Project Goals\n\n- Browse and search candy products by category\n- Manage shopping cart and checkout flow\n- User account creation and order history\n- Secure payment processing (Stripe integration)\n- Admin dashboard for inventory management\n- Email notifications for orders\n- Mobile-responsive design\n\n## 🏗️ Architecture\n\n```\ntest-shop/\n├── backend/          # Node.js/Express API server\n├── frontend/         # React web application\n├── docs/             # Documentation\n└── README.md         # This file\n```\n\n## 🚀 Quick Start\n\nSee [SETUP.md](./SETUP.md) for detailed setup instructions.\n\n### Prerequisites\n- Node.js >= 16.x\n- npm >= 8.x or yarn\n- Git\n\n### Installation (Quick)\n\n```bash\n# Clone repository\ngit clone <repository-url>\ncd test-shop\n\n# Backend setup\ncd backend\nnpm install\ncp .env.example .env\nnpm run dev\n\n# Frontend setup (new terminal)\ncd frontend\nnpm install\nnpm start\n```\n\nBackend runs on `http://localhost:5000`  \nFrontend runs on `http://localhost:3000`\n\n## 📋 Features\n\n### Phase 1: MVP (Current)\n- [x] Project setup and Git workflow\n- [ ] Product catalog and categories\n- [ ] Shopping cart management\n- [ ] Product search and filtering\n- [ ] User authentication\n- [ ] Checkout flow\n- [ ] Payment integration (Stripe)\n\n### Phase 2: Enhancement\n- [ ] Admin dashboard\n- [ ] Order history\n- [ ] Email notifications\n- [ ] Product reviews\n- [ ] Wishlist feature\n\n### Phase 3: Advanced\n- [ ] Advanced analytics\n- [ ] Recommendation engine\n- [ ] Subscription options\n- [ ] Multi-currency support\n\n## 🔧 Tech Stack\n\n### Backend\n- **Runtime:** Node.js\n- **Framework:** Express.js\n- **Database:** PostgreSQL\n- **Authentication:** JWT (planned)\n- **Payment:** Stripe API (planned)\n\n### Frontend\n- **Library:** React 18+\n- **Styling:** CSS Modules / TailwindCSS (planned)\n- **State Management:** React Context API / Redux (planned)\n- **HTTP Client:** Axios\n- **Build Tool:** Create React App\n\n### DevOps\n- **Version Control:** Git\n- **Package Manager:** npm\n- **Environment:** Node.js 16+ LTS\n\n## 📝 Documentation\n\n- **[SETUP.md](./SETUP.md)** - Local development setup guide\n- **[GIT_WORKFLOW.md](./GIT_WORKFLOW.md)** - Git branching strategy and workflow\n- **Backend API** - `backend/README.md` (planned)\n- **Frontend Guide** - `frontend/README.md` (planned)\n\n## 🎓 Development Workflow\n\n1. **Create feature branch** from `develop`:\n   ```bash\n   git checkout -b feature/your-feature-name\n   ```\n\n2. **Make changes** and test locally\n\n3. **Commit with meaningful messages**:\n   ```bash\n   git commit -m \"feat: add product search functionality\"\n   ```\n\n4. **Push and create Pull Request**:\n   ```bash\n   git push origin feature/your-feature-name\n   ```\n\n5. **Code review** → Merge to `develop`\n\nSee [GIT_WORKFLOW.md](./GIT_WORKFLOW.md) for detailed Git strategy.\n\n## 🤝 Contributing\n\nThis is a team project. Please:\n- Follow the Git workflow documented in [GIT_WORKFLOW.md](./GIT_WORKFLOW.md)\n- Write clear commit messages\n- Test changes locally before pushing\n- Request code reviews via pull requests\n- Keep branches up-to-date with `develop`\n\n## 🐛 Troubleshooting\n\n### Backend won't start\n```bash\ncd backend\nrm -rf node_modules package-lock.json\nnpm install\nnpm run dev\n```\n\n### Frontend won't start\n```bash\ncd frontend\nrm -rf node_modules package-lock.json\nnpm install\nnpm start\n```\n\n### Port already in use\n- Backend (5000): Check `backend/.env` for `PORT`\n- Frontend (3000): Check `frontend/.env` for `REACT_APP_PORT`\n\nSee [SETUP.md](./SETUP.md) for more troubleshooting.\n\n## 📞 Support\n\nFor questions or issues:\n1. Check [SETUP.md](./SETUP.md) troubleshooting section\n2. Review [GIT_WORKFLOW.md](./GIT_WORKFLOW.md) for process questions\n3. Open an issue on the repository\n4. Ask team members in Slack/Discord\n\n## 📄 License\n\nTBD\n\n---\n\n**Last Updated:** 2024  \n**Project Status:** Setup Phase ✅ → Feature Development 🚀\n"
  },
  {
    "path": "SETUP.md",
    "content": "# Development Environment Setup Guide\n\n## Table of Contents\n1. [Prerequisites](#prerequisites)\n2. [Repository Cloning](#repository-cloning)\n3. [Backend Setup](#backend-setup)\n4. [Frontend Setup](#frontend-setup)\n5. [Starting Development](#starting-development)\n6. [Verification Checklist](#verification-checklist)\n7. [Troubleshooting](#troubleshooting)\n\n---\n\n## Prerequisites\n\nBefore you begin, ensure you have the following installed:\n\n### Required\n- **Node.js** >= 16.x LTS ([Download](https://nodejs.org/))\n  ```bash\n  node --version  # Should be v16.x or higher\n  ```\n- **npm** >= 8.x (comes with Node.js)\n  ```bash\n  npm --version   # Should be v8.x or higher\n  ```\n- **Git** ([Download](https://git-scm.com/))\n  ```bash\n  git --version\n  ```\n- **Code Editor** (VS Code recommended, but any editor works)\n\n### Recommended\n- **VS Code Extensions:**\n  - ES7+ React/Redux/React-Native snippets\n  - Prettier - Code formatter\n  - ESLint\n  - Thunder Client or Postman for API testing\n\n### Recommended (Not Required Now)\n- **PostgreSQL** 13+ (for database - not needed during this phase)\n- **Docker** (optional, for containerized development)\n\n---\n\n## Repository Cloning\n\n### Step 1: Clone the Repository\n\n```bash\n# SSH (recommended if you have SSH keys set up)\ngit clone git@github.com:your-org/test-shop.git\n\n# HTTPS (if SSH not configured)\ngit clone https://github.com/your-org/test-shop.git\n\ncd test-shop\n```\n\n### Step 2: Verify Git Setup\n\n```bash\n# Check current branch (should be 'main' or 'develop')\ngit branch -a\n\n# Verify remote is configured\ngit remote -v\n```\n\n---\n\n## Backend Setup\n\n### Step 1: Navigate to Backend Directory\n\n```bash\ncd backend\n```\n\n### Step 2: Install Dependencies\n\n```bash\nnpm install\n```\n\nThis reads `package.json` and installs all required packages in `node_modules/`.\n\n### Step 3: Configure Environment Variables\n\n```bash\n# Copy the example environment file\ncp .env.example .env\n```\n\nOpen `.env` in your editor and update if needed:\n\n```env\n# Server Configuration\nNODE_ENV=development\nPORT=5000\nHOST=localhost\n\n# Frontend URL (for CORS)\nFRONTEND_URL=http://localhost:3000\n\n# Database (will be used later)\nDB_HOST=localhost\nDB_PORT=5432\nDB_NAME=testshop_dev\nDB_USER=postgres\nDB_PASSWORD=your_password\n\n# JWT Secret (will be used for authentication)\nJWT_SECRET=your-super-secret-key-change-in-production\nJWT_EXPIRE=7d\n\n# Stripe Keys (will be used for payments)\nSTRIPE_SECRET_KEY=sk_test_your_key_here\nSTRIPE_PUBLIC_KEY=pk_test_your_key_here\n\n# Logging\nLOG_LEVEL=debug\n```\n\n### Step 4: Verify Backend Installation\n\n```bash\n# Check if dependencies are installed\nls -la node_modules | head\n\n# Verify package.json scripts\ncat package.json | grep -A 10 '\"scripts\"'\n```\n\n---\n\n## Frontend Setup\n\n### Step 1: Navigate to Frontend Directory\n\n```bash\n# From test-shop root\ncd frontend\n```\n\n### Step 2: Install Dependencies\n\n```bash\nnpm install\n```\n\nThis may take a few minutes as React and dependencies are large.\n\n### Step 3: Configure Environment Variables\n\n```bash\n# Copy the example environment file\ncp .env.example .env\n```\n\nOpen `.env` in your editor:\n\n```env\n# React App Configuration\nREACT_APP_API_URL=http://localhost:5000/api\nREACT_APP_API_TIMEOUT=30000\nREACT_APP_ENV=development\n\n# Optional: Development server port\nPORT=3000\n```\n\n### Step 4: Verify Frontend Installation\n\n```bash\n# Check if dependencies are installed\nls -la node_modules | head\n\n# Verify React installation\ncat package.json | grep -A 5 'dependencies'\n```\n\n---\n\n## Starting Development\n\n### Option 1: Two Terminal Windows (Recommended for Beginners)\n\n**Terminal 1 - Backend:**\n```bash\ncd test-shop/backend\nnpm run dev\n\n# Output should show:\n# ✓ Server running on http://localhost:5000\n# ✓ Environment: development\n```\n\n**Terminal 2 - Frontend:**\n```bash\ncd test-shop/frontend\nnpm start\n\n# Output should show:\n# Compiled successfully!\n# Local:            http://localhost:3000\n```\n\n### Option 2: Root-level Scripts (If Configured)\n\n```bash\n# From test-shop root\nnpm run dev:all\n\n# Or individually:\nnpm run dev:backend\nnpm run dev:frontend\n```\n\n### Verify Both Are Running\n\n**Backend Health Check:**\n```bash\ncurl http://localhost:5000/api/health\n# Should return: {\"status\":\"ok\",\"timestamp\":\"...\",\"uptime\":...}\n```\n\n**Frontend Access:**\n- Open browser: http://localhost:3000\n- You should see the React app loaded\n\n---\n\n## Verification Checklist\n\nBefore proceeding with development, verify:\n\n- [ ] Node.js and npm installed correctly\n- [ ] Repository cloned successfully\n- [ ] Backend dependencies installed (`node_modules/` exists)\n- [ ] Frontend dependencies installed (`node_modules/` exists)\n- [ ] `.env` files created in both backend and frontend\n- [ ] Backend starts without errors on `http://localhost:5000`\n- [ ] Frontend starts without errors on `http://localhost:3000`\n- [ ] Backend health endpoint responds: `curl http://localhost:5000/api/health`\n- [ ] Browser shows React app at `http://localhost:3000`\n- [ ] Console shows no critical errors\n\n---\n\n## Troubleshooting\n\n### Issue: `npm: command not found`\n\n**Solution:** Node.js not installed or not in PATH\n```bash\n# Download and install from https://nodejs.org/\nnode --version\nnpm --version\n```\n\n### Issue: `EADDRINUSE: address already in use :::5000`\n\n**Solution:** Port 5000 is already in use\n\n```bash\n# Find process using port 5000\nlsof -i :5000          # macOS/Linux\nnetstat -ano | findstr :5000  # Windows\n\n# Kill the process or use a different port\n# Option A: Kill process\nkill -9 <PID>          # macOS/Linux\n\n# Option B: Use different port\ncd backend\necho \"PORT=5001\" >> .env\nnpm run dev\n```\n\n### Issue: `npm install` hangs or fails\n\n**Solution:** Clear cache and retry\n\n```bash\n# Clear npm cache\nnpm cache clean --force\n\n# Remove node_modules and lock file\nrm -rf node_modules package-lock.json\n\n# Try installing again\nnpm install\n\n# If still failing, try with legacy peer deps\nnpm install --legacy-peer-deps\n```\n\n### Issue: Backend starts but frontend can't connect\n\n**Solution:** Check CORS configuration\n\n```bash\n# In backend/.env, verify FRONTEND_URL is correct\necho $FRONTEND_URL  # Should output: http://localhost:3000\n\n# Check backend logs for CORS errors\n# Restart both services\ncd backend && npm run dev\n# In another terminal:\ncd frontend && npm start\n```\n\n### Issue: `React is not defined` or module errors\n\n**Solution:** Reinstall frontend dependencies\n\n```bash\ncd frontend\nrm -rf node_modules package-lock.json\nnpm install\nnpm start\n```\n\n### Issue: Backend crashes with \"Cannot find module\"\n\n**Solution:** Missing middleware or incorrect path\n\n```bash\ncd backend\n\n# Check if all middleware files exist\nls -la src/middleware/\n\n# Reinstall dependencies\nrm -rf node_modules package-lock.json\nnpm install\n\nnpm run dev\n```\n\n### Issue: Frontend shows blank page\n\n**Solution:** Check browser console for errors\n\n1. Open DevTools (F12)\n2. Check Console tab for errors\n3. Check Network tab to see if API calls succeed\n4. Ensure `REACT_APP_API_URL` in `.env` matches backend URL\n\n### Issue: Git shows uncommitted changes from setup\n\n**Solution:** This is normal. The `.env` files should not be committed.\n\n```bash\n# Verify .env files are in .gitignore\ncat .gitignore | grep \"\\.env\"\n\n# Check git status\ngit status\n\n# Only node_modules/ and .env should be untracked (red)\n# This is fine!\n```\n\n---\n\n## Next Steps\n\nOnce your development environment is set up:\n\n1. **Create a feature branch:**\n   ```bash\n   git checkout -b feature/your-feature-name\n   ```\n\n2. **Review Git workflow:**\n   - Read [GIT_WORKFLOW.md](../GIT_WORKFLOW.md)\n\n3. **Start development:**\n   - Backend: `backend/src/` contains API code\n   - Frontend: `frontend/src/` contains React components\n\n4. **Make commits:**\n   ```bash\n   git add .\n   git commit -m \"feat: description of your changes\"\n   git push origin feature/your-feature-name\n   ```\n\n5. **Create Pull Request** for code review\n\n---\n\n## Quick Reference\n\n### Common Commands\n\n```bash\n# Backend\ncd backend\nnpm run dev          # Start development server\nnpm run start        # Start production server\nnpm test             # Run tests (when available)\n\n# Frontend\ncd frontend\nnpm start            # Start development server\nnpm run build        # Build for production\nnpm test             # Run tests (when available)\n\n# Git\ngit checkout -b feature/name      # Create feature branch\ngit add .                          # Stage changes\ngit commit -m \"message\"            # Commit\ngit push origin feature/name       # Push to remote\ngit pull origin develop            # Sync with develop\n```\n\n### Environment Files\n\n```bash\n# Backend\nbackend/.env                  # Your local config (NEVER commit)\nbackend/.env.example          # Template (commit this)\n\n# Frontend\nfrontend/.env                 # Your local config (NEVER commit)\nfrontend/.env.example         # Template (commit this)\n```\n\n---\n\n## Getting Help\n\n1. **Check this guide** - Most common issues are covered above\n2. **Check README.md** - Project overview and links\n3. **Check GIT_WORKFLOW.md** - For Git-related questions\n4. **Ask team members** - Slack/Discord/Email\n5. **Search Google** - Generic Node.js/React errors\n6. **Check logs** - Both backend and frontend show detailed error messages\n\n---\n\n**Last Updated:** 2024  \n**Status:** ✅ Ready for development\n"
  },
  {
    "path": "GIT_WORKFLOW.md",
    "content": "# Git Workflow & Branching Strategy\n\nThis document defines the Git branching strategy and workflow for the TEST SHOP project.\n\n## Overview\n\nWe follow a **modified Git Flow** with the following main branches:\n\n- **`main`** - Production-ready code (stable releases)\n- **`develop`** - Integration branch for features (staging environment)\n- **`feature/*`** - Feature branches (from develop)\n- **`bugfix/*`** - Bugfix branches (from develop)\n- **`hotfix/*`** - Emergency production fixes (from main)\n\n---\n\n## Branch Rules\n\n### Main Branches\n\n#### `main`\n- **Purpose:** Production code, always stable and tested\n- **Protection:** Yes - requires PR reviews and passing tests\n- **Merge from:** `hotfix/*`, `release/*` (via PR)\n- **Tagged:** Every production release (v1.0.0, v1.0.1, etc.)\n- **Who can merge:** Project maintainers only\n\n#### `develop`\n- **Purpose:** Integration branch, newest features and fixes\n- **Protection:** Yes - requires PR reviews\n- **Merge from:** `feature/*`, `bugfix/*` (via PR)\n- **Who can merge:** Team leads\n- **Deploy to:** Staging environment\n\n### Supporting Branches\n\n#### `feature/*`\n- **Purpose:** New features or enhancements\n- **Naming:** `feature/feature-name` (kebab-case)\n- **Created from:** `develop`\n- **Merged back to:** `develop` (via PR)\n- **Examples:**\n  - `feature/product-catalog`\n  - `feature/shopping-cart`\n  - `feature/user-authentication`\n  - `feature/payment-integration`\n\n#### `bugfix/*`\n- **Purpose:** Bug fixes for non-critical issues\n- **Naming:** `bugfix/bug-description` (kebab-case)\n- **Created from:** `develop`\n- **Merged back to:** `develop` (via PR)\n- **Examples:**\n  - `bugfix/cart-calculation-error`\n  - `bugfix/search-filter-bug`\n  - `bugfix/mobile-responsive-issue`\n\n#### `hotfix/*`\n- **Purpose:** Critical production bugs that need immediate fix\n- **Naming:** `hotfix/issue-description` (kebab-case)\n- **Created from:** `main`\n- **Merged back to:** `main` AND `develop` (via separate PRs)\n- **Examples:**\n  - `hotfix/payment-processor-down`\n  - `hotfix/security-vulnerability`\n  - `hotfix/critical-data-loss`\n\n---\n\n## Workflow\n\n### Starting a New Feature\n\n```bash\n# 1. Update local develop branch\ngit checkout develop\ngit pull origin develop\n\n# 2. Create feature branch from develop\ngit checkout -b feature/your-feature-name\n\n# 3. Work on your feature\n# ... edit files, commit regularly ...\n\n# 4. Make meaningful commits\ngit add src/components/ProductCard.js\ngit commit -m \"feat: add product card component\"\n\ngit add src/pages/ProductPage.js\ngit commit -m \"feat: create product page layout\"\n\n# 5. Push to remote\ngit push -u origin feature/your-feature-name\n\n# 6. Create Pull Request on GitHub/GitLab\n# Title: Add Product Card Component\n# Description: Describe what you added and why\n# Link to any related issues\n```\n\n### Commit Message Conventions\n\nUse **Conventional Commits** format:\n\n```\n<type>(<scope>): <subject>\n\n<body>\n\n<footer>\n```\n\n**Types:**\n- `feat:` - New feature\n- `fix:` - Bug fix\n- `docs:` - Documentation changes\n- `style:` - Code style changes (formatting, semicolons, etc.)\n- `refactor:` - Code refactoring without behavior change\n- `test:` - Adding or updating tests\n- `chore:` - Build process, dependencies, etc.\n- `ci:` - CI/CD configuration\n- `perf:` - Performance improvements\n\n**Examples:**\n\n```bash\n# Good\ngit commit -m \"feat(cart): add item quantity update functionality\"\ngit commit -m \"fix(auth): resolve JWT token expiration issue\"\ngit commit -m \"docs: update setup instructions\"\ngit commit -m \"refactor(api): simplify product service\"\ngit commit -m \"test(checkout): add payment processing tests\"\n\n# Less good\ngit commit -m \"fix stuff\"\ngit commit -m \"update code\"\ngit commit -m \"changes\"\n```\n\n### Creating a Pull Request\n\n1. **Push your feature branch:**\n   ```bash\n   git push origin feature/your-feature-name\n   ```\n\n2. **Go to GitHub/GitLab** and click \"Create Pull Request\"\n\n3. **Fill in PR details:**\n   ```markdown\n   ## Description\n   Brief description of the changes.\n\n   ## Related Issue\n   Closes #123\n\n   ## Changes\n   - Added product card component\n   - Added product detail page\n   - Updated product service\n\n   ## Type of Change\n   - [x] New feature (non-breaking)\n   - [ ] Bug fix (non-breaking)\n   - [ ] Breaking change\n   - [ ] Documentation update\n\n   ## Testing\n   - [x] Tested locally\n   - [x] Components render correctly\n   - [x] API calls work as expected\n\n   ## Screenshots (if applicable)\n   Add screenshots of UI changes.\n   ```\n\n4. **Request reviewers** - Assign team members\n\n5. **Address feedback** - Respond to comments and make changes\n\n### Merging a Pull Request\n\n**Requirements:**\n- [ ] Code review approved by at least 1 team member\n- [ ] All CI/CD checks passing\n- [ ] Branch is up-to-date with `develop`\n- [ ] No conflicts\n\n**Steps:**\n```bash\n# Pull latest develop to ensure no conflicts\ngit checkout develop\ngit pull origin develop\n\n# Merge feature branch\ngit merge feature/your-feature-name\n\n# Or via GitHub/GitLab UI\n# Click \"Merge Pull Request\" button\n\n# After merge, delete feature branch\ngit branch -d feature/your-feature-name\ngit push origin --delete feature/your-feature-name\n```\n\n### Keeping Your Branch Updated\n\nIf `develop` has new commits while you're working:\n\n```bash\n# Option 1: Rebase (recommended, cleaner history)\ngit fetch origin\ngit rebase origin/develop\n\n# If there are conflicts, resolve them:\n# ... fix conflict markers in files ...\ngit add .\ngit rebase --continue\n\n# Option 2: Merge (simpler, creates merge commit)\ngit fetch origin\ngit merge origin/develop\n```\n\n### Handling Production Hotfixes\n\n```bash\n# 1. Create hotfix branch from main\ngit checkout main\ngit pull origin main\ngit checkout -b hotfix/critical-issue\n\n# 2. Fix the issue\n# ... make changes ...\ngit add .\ngit commit -m \"fix: critical payment processor issue\"\n\n# 3. Push and create PR to main\ngit push -u origin hotfix/critical-issue\n# Create PR to main (not develop!)\n\n# 4. Once merged to main, merge to develop too\ngit checkout develop\ngit pull origin develop\ngit merge main\ngit push origin develop\n```\n\n---\n\n## Common Scenarios\n\n### Scenario 1: Feature Review Feedback\n\n```bash\n# 1. Get latest feedback\ngit pull origin feature/your-feature\n\n# 2. Make requested changes\n# ... edit files ...\n\n# 3. Commit and push (no new PR needed)\ngit add .\ngit commit -m \"feat: address review feedback\"\ngit push origin feature/your-feature\n\n# 4. Comment on PR: \"Updated based on feedback\"\n```\n\n### Scenario 2: Accidental Commit to Wrong Branch\n\n```bash\n# You committed to develop by mistake\n# Get the commit hash\ngit log -1  # Note the commit hash\n\n# Revert on develop\ngit revert <commit-hash>\ngit push origin develop\n\n# Apply to correct feature branch\ngit checkout feature/your-feature\ngit cherry-pick <commit-hash>\ngit push origin feature/your-feature\n```\n\n### Scenario 3: Large Feature Needs Multiple Commits\n\n```bash\n# Just commit normally\ngit checkout -b feature/large-feature\n\n# First part\ngit add src/models/Product.js\ngit commit -m \"feat: add product model\"\n\n# Second part\ngit add src/controllers/ProductController.js\ngit commit -m \"feat: add product controller\"\n\n# Third part\ngit add src/routes/products.js\ngit commit -m \"feat: add product routes\"\n